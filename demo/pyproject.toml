[project]
name = "django-dagster-demo"
version = "0.1.0"
description = "Demo project for django-dagster"
requires-python = ">=3.13"
dependencies = [
    "django>=5.1",
    "django-dagster",
]

[tool.uv.sources]
django-dagster = { path = ".." }
